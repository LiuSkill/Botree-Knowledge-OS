# Dashboard Module

## 功能

首页工作台用于展示欢迎区、核心指标、快捷入口、知识资产分布、近 7 天 AI 问答趋势和文档类型分布。

## 调用关系

路由模块将 `/dashboard` 指向 `DashboardPage.vue`。页面调用 `getDashboardStats()` 读取 `/api/system/dashboard`，并使用 `ROUTE_PATHS` 统一处理跳转路径。

## 输入

- 后端工作台统计响应：项目数量、文档数量、知识条目数、AI 回答次数和待办审核数。
- 后端统计数据：权限范围内的知识资产分布、AI 问答趋势和文档类型分布。
- 当前登录用户信息：用于欢迎区展示姓名。

## 输出

- 首页工作台页面。
- 知识中心、项目中心、企业知识问答、项目知识问答和问答审计入口。
- 后端为空时展示空状态，不补充前端样例数据。

## 示例

```vue
<DashboardPage />
```
