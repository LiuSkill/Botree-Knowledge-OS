第 1 步：搭建全局布局和路由
请实现 Botree Knowledge OS 的全局布局和路由。

要求：
1. 创建 MainLayout。
2. 顶部导航包含：Logo、全局搜索、语言切换、通知、用户信息。
3. 左侧菜单包含：首页、知识中心、项目中心、知识授权中心、AI中心、系统管理。
4. 点击菜单跳转对应页面，并高亮当前菜单。
5. 创建以下路由：
   /dashboard
   /knowledge
   /projects
   /projects/:id
   /authorization
   /ai
   /system
6. 默认重定向到 /dashboard。
7. 所有页面先用占位内容。
8. 使用 TDesign React 的 Layout、Menu、Input、Dropdown、Avatar、Badge 等组件。
9. 保持企业级SaaS风格：白底、蓝色主色、左侧菜单、顶部导航。

完成后运行构建检查。
第 2 步：建立 Mock 数据和全局状态
请建立全局 mock 数据和状态管理。

使用 Zustand 创建 store。

需要 mock 以下数据：

1. 用户信息
   - 姓名
   - 角色
   - 部门
   - 头像

2. 知识文档列表
   字段：
   id、title、category、tags、author、uploadTime、fileSize、parseStatus、securityLevel、aiEnabled、summary、version

3. 项目列表
   字段：
   id、name、client、manager、status、documentCount、knowledgeCount、progress、updateTime

4. 项目资料
   字段：
   id、projectId、folder、fileName、type、version、uploader、uploadTime、externalVisible

5. 项目成员
   字段：
   id、projectId、name、company、role、permission、expireAt、status、userType

6. 授权配置
   字段：
   projectId、knowledgeCategory、level、externalVisible、expireAt、status

7. AI会话
   字段：
   id、mode、expertType、projectId、title、messages、sources

要求：
1. 数据可增删改。
2. 所有页面后续都从 store 读取数据。
3. 提供基础 action：add、update、remove、toggle。
4. 不接后端API。
第 3 步：首页 Dashboard
请实现首页工作台 Dashboard 页面。

参考原型实现页面视觉和交互。

页面内容：
1. 欢迎区：
   欢迎回来，显示当前用户姓名、今日待办提示。

2. KPI卡片：
   - 知识文档数
   - 知识条目数
   - 项目数量
   - AI问答次数

3. 快捷入口：
   - 上传文档
   - 新建项目
   - 企业知识问答
   - 项目知识问答

4. 最近上传文档列表。
5. 最近AI问答列表。
6. 知识建设进度。

交互要求：
1. 点击知识文档KPI跳转 /knowledge。
2. 点击项目KPI跳转 /projects。
3. 点击AI问答KPI跳转 /ai。
4. 点击上传文档打开上传文档弹窗。
5. 点击新建项目打开新建项目弹窗。
6. 点击企业知识问答跳转 /ai?mode=enterprise。
7. 点击项目知识问答跳转 /ai?mode=project。
8. 最近上传文档点击后打开文档详情抽屉。
9. 所有弹窗、抽屉、Toast 使用 TDesign React 组件。

完成后运行构建检查。
第 4 步：知识中心基础页面
请实现知识中心 KnowledgeCenter 页面。

当前只实现“知识文档管理”，暂不实现知识条目、实验报告、专家经验、术语库。

页面布局：
1. 左侧：知识分类树。
2. 中间：文档列表。
3. 右侧：文档详情抽屉。

知识分类包括：
电池基础、LFP回收、NCM回收、黑粉处理、湿法冶金、工艺设计、工艺开发、设备、EHS、标准规范。

文档列表字段：
文档名称、分类、标签、上传人、上传时间、文件大小、解析状态、密级、是否参与AI问答、操作。

功能：
1. 点击分类刷新文档列表。
2. 搜索文档标题。
3. 按分类、密级、解析状态筛选。
4. 分页。
5. 点击文档名称打开详情抽屉。
6. AI问答开关可切换。
7. 操作包含：预览、编辑、下载、归档、删除。
8. 删除必须二次确认。
9. 编辑打开表单弹窗。

使用 TDesign React 的 Tree、Table、Tag、Switch、Drawer、Dialog、Form、Button、Pagination。
第 5 步：知识中心上传与详情
请补充知识中心的上传文档和详情能力。

上传文档弹窗字段：
1. 文件上传区域
2. 所属分类
3. 所属项目，可为空
4. 标签
5. 密级：Public、Internal、Confidential
6. 是否参与AI问答
7. 文档摘要

交互：
1. 上传后新增一条 mock 文档。
2. 显示上传成功 Toast。
3. 表单必填校验：文件、分类、密级。

文档详情抽屉展示：
1. 文档名称
2. 摘要
3. 元数据
4. 标签
5. 版本
6. 解析状态
7. 向量化状态
8. 关联文档
9. 引用记录

预览按钮打开预览弹窗。
下载按钮显示下载成功 Toast。
归档按钮将文档状态更新为已归档。
第 6 步：项目中心列表
请实现项目中心 ProjectCenter 页面。

页面内容：
1. 顶部标题和说明。
2. 项目搜索框。
3. 项目状态筛选：全部、进行中、已完成、待启动、已归档。
4. 新建项目按钮。
5. 项目卡片列表。

项目卡片字段：
项目名称、客户、负责人、状态、文档数、知识数、进度、更新时间。

交互：
1. 点击项目卡片进入 /projects/:id。
2. 点击新建项目打开项目创建弹窗。
3. 支持编辑项目。
4. 支持归档项目。
5. 支持删除项目，删除二次确认。
6. 状态筛选即时刷新列表。
7. 搜索框支持项目名称和客户搜索。

使用 TDesign React 的 Card、Button、Tag、Progress、Dialog、Form、Input、Select。
第 7 步：项目详情基础页
请实现项目详情 ProjectDetail 页面。

页面结构：
1. 顶部返回按钮。
2. 项目名称、客户、负责人、状态。
3. Tab标签页：
   - 概览
   - 项目资料
   - 项目成员
   - 项目知识库
   - 项目问答
   - 授权管理

概览Tab内容：
1. 项目进度
2. 文档数量
3. 知识数量
4. 成员数量
5. 项目里程碑
6. 最近活动

交互：
1. 返回按钮回到 /projects。
2. Tab可切换。
3. 授权管理Tab中提供按钮跳转到 /authorization?projectId=当前项目。
4. 项目问答Tab中提供按钮跳转 /ai?mode=project&projectId=当前项目。

使用 TDesign React Tabs、Card、Progress、Timeline。
第 8 步：项目资料 Tab
请实现项目详情中的“项目资料”Tab。

布局：
1. 左侧为项目资料目录树。
2. 右侧为当前目录文件列表。

标准目录：
01 基础资料
02 工艺设计
03 图纸
04 设备资料
05 会议纪要
06 实验报告
07 交付文件
08 EHS资料

文件列表字段：
文件名、类型、版本、上传人、上传时间、外部可见、操作。

功能：
1. 点击目录刷新文件列表。
2. 上传资料打开上传弹窗。
3. 新建文件夹打开弹窗。
4. 点击文件名打开资料详情抽屉。
5. 预览显示弹窗。
6. 下载显示Toast。
7. 版本管理打开版本弹窗。
8. 外部可见使用Switch切换。
9. 打包导出显示成功Toast。

使用 Tree、Table、Switch、Drawer、Dialog、Upload。
第 9 步：项目成员 Tab
请实现项目详情中的“项目成员”Tab。

成员类型：
内部成员、外部客户、供应商、EPC合作方。

列表字段：
姓名、单位、角色、权限、有效期、状态、用户类型、操作。

功能：
1. 添加成员打开表单弹窗。
2. 编辑成员打开表单弹窗。
3. 移除成员二次确认。
4. 设置角色。
5. 设置有效期。
6. 查看权限打开权限详情抽屉。
7. 外部用户权限详情中明确展示：仅可访问本项目资料和授权知识。

角色包括：
项目管理员、项目成员、只读成员、外部用户。

使用 Table、Dialog、Form、Select、DatePicker、Drawer、Tag。
第 10 步：项目知识库 / 项目问答 / 授权Tab
请补充项目详情中的三个Tab。

1. 项目知识库Tab：
展示项目文档、项目FAQ、项目知识摘要。
支持搜索、查看详情、是否可用于项目问答开关。

2. 项目问答Tab：
展示项目问答入口、最近项目问答记录。
点击“进入项目问答”跳转 /ai?mode=project&projectId=当前项目。

3. 授权管理Tab：
展示当前项目已授权的专业知识分类。
字段：分类、授权级别、外部可见、有效期、状态。
点击“配置授权”跳转 /authorization?projectId=当前项目。

暂不需要复杂拒答逻辑。
第 11 步：知识授权中心
请实现知识授权中心 KnowledgeAuthorization 页面。

这是“项目继承专业知识”的授权中心，不是普通用户权限表。

布局：
1. 左侧：项目列表。
2. 中间：专业知识分类树。
3. 右侧：授权配置面板。

专业知识分类：
电池基础、LFP、NCM、黑粉、湿法冶金、工艺设计、工艺开发、设备、EHS、标准规范。

每个分类有密级：
Public、Internal、Confidential。

规则：
1. Public 可直接勾选授权。
2. Internal 勾选后显示“需要审批”提示。
3. Confidential 置灰，不可勾选。

交互：
1. 点击项目切换当前项目授权配置。
2. 勾选分类更新右侧已授权列表。
3. 设置外部可见。
4. 设置授权有效期。
5. 保存授权显示成功Toast。
6. 撤销授权二次确认。
7. 权限预览打开抽屉。
8. 审计日志使用Table展示。
第 12 步：AI中心基础
请实现 AI中心 AICenter 页面。

布局：
1. 左侧：历史会话列表。
2. 中间：聊天窗口。
3. 右侧：引用来源和相关知识。

模式：
1. 企业专家模式
2. 项目专家模式

企业专家包括：
工艺专家、研发专家、设备专家、EHS专家。

项目专家：
需要选择当前项目。

功能：
1. 支持企业专家/项目专家模式切换。
2. 支持专家切换。
3. 支持选择项目。
4. 根据 URL query 初始化 mode 和 projectId。
5. 左侧会话点击后加载会话。
6. 新建会话按钮可创建空会话。
7. 页面清晰显示当前知识范围。
第 13 步：AI问答交互
请补充 AI中心问答交互。

功能：
1. 输入问题并发送。
2. 发送后显示用户消息。
3. 显示AI加载状态。
4. 1秒后显示mock AI回答。
5. AI回答包含引用来源。
6. 引用来源可展开/收起。
7. 点击引用来源打开文档详情抽屉。
8. 右侧相关知识根据问题显示相关文档。
9. 支持复制答案。
10. 支持点赞、点踩。
11. 支持反馈弹窗。
12. 暂不实现保存为知识条目。
13. 暂不实现项目知识拒答逻辑。

使用 TDesign React 的 Textarea、Button、Drawer、Collapse、Loading、MessagePlugin。
第 14 步：系统管理
请实现系统管理 SystemManagement 页面。

页面使用 Tab：
1. 用户管理
2. 角色管理
3. 权限矩阵
4. 部门管理
5. 操作日志
6. 问答审计

用户管理：
字段：姓名、账号、邮箱、部门、角色、状态、创建时间。
支持新增、编辑、禁用、删除、重置密码、分配角色、分配项目。

角色管理：
支持新增角色、编辑角色、删除角色、配置权限。

权限矩阵：
按模块展示权限：查看、新增、编辑、删除、审核、授权。

操作日志：
按用户、时间、操作类型筛选，支持查看详情。

问答审计：
字段：问题、回答摘要、引用来源、用户、时间、模式。

使用 Table、Tabs、Dialog、Form、Tag、DateRangePicker。
第 15 步：中英文切换
请实现中英文切换。

要求：
1. 默认中文。
2. 顶部语言按钮可切换中文/English。
3. 使用 Zustand 保存当前语言。
4. 建立简单 i18n 字典文件，不引入复杂国际化库。
5. 至少覆盖：
   - 左侧菜单
   - 顶部导航
   - 页面标题
   - 主要按钮
   - 主要Tab
   - 表格列名
6. 切换语言后当前页面立即刷新文本。
7. 不要求 mock 数据内容全部翻译，只需要界面文本支持切换。
第 16 步：统一弹窗、抽屉、反馈
请统一全局交互体验。

要求：
1. 所有新增、编辑表单使用统一 Dialog 风格。
2. 所有详情使用右侧 Drawer。
3. 所有删除操作使用二次确认。
4. 所有保存、上传、下载、归档、授权操作都有 MessagePlugin Toast。
5. 表单必填项校验。
6. 所有空列表显示 Empty 组件。
7. 所有加载状态显示 Loading。
8. 所有按钮不能无响应。
9. 统一圆角、间距、卡片阴影，风格接近原型。
10. 检查并补齐无交互按钮。
第 17 步：最终自检和修复
请对整个前端项目进行最终自检和修复。

检查内容：
1. npm install 是否成功。
2. npm run dev 是否可启动。
3. npm run build 是否通过。
4. TypeScript 是否无明显错误。
5. 所有路由是否可访问。
6. 左侧菜单是否可跳转并高亮。
7. 首页所有快捷入口是否可用。
8. 知识中心搜索、筛选、分页、抽屉、弹窗是否可用。
9. 项目中心卡片、详情页、Tab是否可用。
10. 项目资料上传、预览、下载、版本、外部可见是否有交互。
11. 项目成员增删改查是否可用。
12. 知识授权中心是否符合“项目继承专业知识”的业务逻辑。
13. AI中心是否可发送问题并展示回答、引用来源。
14. 系统管理各Tab是否可交互。
15. 中英文切换是否可用。
16. 删除是否二次确认。
17. 不允许出现无响应按钮。

最后输出：
- 已完成页面清单
- 已完成交互清单
- 未完成项
- 启动命令
- 构建结果