# 会话级短期记忆作为会话状态而非证据层

问答模块新增会话级短期记忆时，采用“最近几轮原文窗口 + 持久化结构化记忆快照”的混合模型，并将结构化记忆挂载到 `chat_sessions` 的会话状态中，而不是设计成独立证据来源或独立记忆实体。该记忆同时服务 `project_chat` 与 `base_chat`，但只用于检索前的问题上下文化、指代补全与追问承接；其中待验证上下文不得直接作为回答事实，项目问答的最终结论仍必须受项目资料证据约束，这样可以在保持会话连续性的同时不放松现有证据与权限边界。
